I"6<h4>Nella said "Love is complated, but web-dev is much more."</h4>

<p>How it works when it involved in a love triangle with python, django and heroku?</p>

<p>Here’s the story:</p>
<ol>
  <li>python
    <ul>
      <li>Assume python3.x, venv, django are already installed</li>
      <li>Create a project that contains all the stuff we will mention later, here use “UnchainedDjango” as our project name</li>
      <li>Oepn CLI and change directory to “UnchainedDjango/” and type</li>
    </ul>
    <pre class="brush: bash">
     django-admin startproject UnchainedDjango
     cd UnchainedDjango
 </pre>
    <ul>
      <li>keeping use UnchainedDjango as project name, and you will get a tree like
        <pre class="brush: bash">
  .(UnchainedDjango) &lt;- root dir, "git init" here for heroku deployment
  ├── manage.py
  └── unchaineddjango
      ├── __init__.py
      ├── asgi.py
      ├── settings.py
      ├── urls.py
      └── wsgi.py
  </pre>
      </li>
    </ul>
  </li>
  <li>django
    <ul>
      <li>You have to modify somewhere in</li>
    </ul>
  </li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">setting</span><span class="o">.</span><span class="n">py</span>
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>	- DEBUG = False 3. heroku deploy
- You will need three files:
	- Procfile
		- just create the file named Procfile without any extension
		- open Procfile and insert following command:
			&lt;pre class="brush: bash"&gt;web: gunicorn --pythonpath mysite mysite.wsgi --log-file - &lt;/pre&gt;
	- runtime.txt
		- insert your python version, e.g. mine is 3.6.5
			&lt;pre class="brush: bash"&gt;python-3.6.5&lt;/pre&gt;
	- requirements.txt
		- type at root dir
			&lt;pre class="brush: bash"&gt; pip3 freeze &gt; requirements.txt&lt;/pre&gt;
		- then open "requirements.txt" and insert this package in the file
			&lt;pre class="brush: bash"&gt;psycopg2&lt;/pre&gt;
</code></pre></div></div>
:ET