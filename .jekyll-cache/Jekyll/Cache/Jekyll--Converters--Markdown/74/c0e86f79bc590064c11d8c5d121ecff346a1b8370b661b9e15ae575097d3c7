I"�<h4>Nella said "Love is complated, but web-dev is much more."</h4>

<p>How it works when it involved in a love triangle with python, django and heroku?</p>

<p>Here’s the story:</p>
<ol>
  <li>python
    <ul>
      <li>Assume python3.x, venv, django are already installed</li>
      <li>Create a project that contains all the stuff we will mention later, here use “UnchainedDjango” as our project name</li>
      <li>Oepn CLI and change directory to “UnchainedDjango/” and type</li>
    </ul>
  </li>
</ol>

<pre class="brush: bash line-numbers language-bash">
	<code class=" language-bash">django-admin startproject UnchainedDjango
		<span class="token builtin class-name">cd</span> UnchainedDjango
		<span aria-hidden="true" class="line-numbers-rows">
			<span>
				
			</span>
			<span>
				
			</span>
		</span>
	</code>
</pre>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- keeping use UnchainedDjango as project name, and you will get a tree like

	&lt;pre class="brush: bash"&gt;
	.(UnchainedDjango)  &lt;- "git init" here
	├── manage.py
	└── unchaineddjango
	    ├── __init__.py
	    ├── asgi.py
	    ├── settings.py
	    ├── urls.py
	    └── wsgi.py
	&lt;/pre&gt;

- After pass the command, you will get a  2. django 3. heroku deploy
</code></pre></div></div>

<p>My solution:</p>

<ul>
  <li>Runtime: <strong>164 ms</strong>, faster than <strong>55.49%</strong> of Python3 online submissions</li>
  <li>Memory Usage: <strong>12.2 MB</strong>, less than <strong>100.00%</strong> of Python3 online</li>
</ul>

<p><strong>Ideas</strong>: Square the integer in list then sort the list.</p>
:ET